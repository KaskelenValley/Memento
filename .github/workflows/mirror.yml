name: Mirroring

on:
  push:
    branches:
      - main
      - dev
  delete:

jobs:
  to_github:
    if: github.repository != 'SuleymanDemirelKazakhstan/diploma-project-charming-hamsters'
    runs-on: ubuntu-latest
    steps: # <-- must use actions/checkout before mirroring!
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: weeebdev/repository-mirroring-action@f883c033359a54dc6b2f12ecb291a83d8621fc9d
        with:
          target_repo_url: git@github.com:${{ secrets.USERNAME }}/${{ secrets.REPO_NAME}}.git
          ssh_private_key: # <-- use 'secrets' to pass credential information.
            ${{ secrets.SSH_PRIVATE_KEY }}
